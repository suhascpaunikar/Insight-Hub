/* Port in progress — this screen still has to come across from the vanilla
   build. Replaced in a later commit. */
import { Empty } from '@cloudflare/kumo';

export function Insights() {
  return <Empty>Insights — porting.</Empty>;
}
